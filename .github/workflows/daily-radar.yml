name: daily-radar

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run without publishing or updating state"
        required: false
        default: false
        type: boolean
      report_limit:
        description: "Limit the number of report items for a manual run"
        required: false
        default: "0"
        type: string
      skip_radar:
        description: "Skip GitHub Radar (only run AI Builders)"
        required: false
        default: false
        type: boolean
      skip_ai_builders:
        description: "Skip AI Builders digest"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: github-daily-radar
  cancel-in-progress: false

jobs:
  radar:
    if: ${{ !inputs.skip_radar }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Restore state from state branch
        run: |
          if git show-ref --verify --quiet refs/remotes/origin/state; then
            echo "Restoring artifacts from state branch..."
            git checkout origin/state -- artifacts/ 2>/dev/null || echo "No artifacts in state branch yet"
          else
            echo "No state branch found, starting fresh."
          fi
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --extra dev
      - run: uv run python -m github_daily_radar.main
        env:
          DRY_RUN: ${{ inputs.dry_run || false }}
          REPORT_LIMIT: ${{ inputs.report_limit || 0 }}
          TIMEZONE: Asia/Shanghai
          LLM_MODEL: qwen3.5-plus
          LLM_FALLBACK_MODEL: kimi-k2.5
          CODE_SEARCH_REQUESTS_PER_MINUTE: 4
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_PAT: ${{ secrets.GITHUB_PAT }}
          QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
      - run: bash scripts/sync_state_branch.sh
        if: ${{ !inputs.dry_run }}
      - name: Send failure alert
        if: failure()
        run: uv run python -m github_daily_radar.main --alert-only
        env:
          LLM_MODEL: qwen3.5-plus
          LLM_FALLBACK_MODEL: kimi-k2.5
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_PAT: ${{ secrets.GITHUB_PAT }}
          QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: daily-radar-artifacts
          path: artifacts/

  ai-builders:
    if: ${{ always() && !inputs.skip_ai_builders }}
    runs-on: ubuntu-latest
    needs: [radar]
    # Run even if radar was skipped (skip_radar=true) or failed
    # But don't run if skip_ai_builders=true
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --extra dev
      - name: Run AI Builders Digest
        run: uv run python -m github_daily_radar.ai_builders.main
        env:
          DRY_RUN: ${{ inputs.dry_run || false }}
          TIMEZONE: Asia/Shanghai
          LLM_MODEL: qwen3.5-plus
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
          VOLC_API_KEY: ${{ secrets.VOLC_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
